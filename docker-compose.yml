version: '3.9'

services:
  bot:
    build:
      context: .
      dockerfile: docker/Dockerfile.bot
    container_name: moderation_bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
